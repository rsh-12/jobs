server:
  port: 7002
  error:
    include-message: always

spring:
  application:
    name: resume
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/job
    username: ${DB_USERNAME}
    password: ${DB_PWD}

logging:
  level:
    root: info
    ru.rsh12: debug
