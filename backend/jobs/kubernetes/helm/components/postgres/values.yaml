fullnameOverride: postgres

image:
  repository: registry.hub.docker.com/library
  name: postgres
  tag: 14
  pullPolicy: IfNotPresent
  init: true
ports:
  - containerPort: 5432

service:
  ports:
    - port: 5432