---
# Source: prod-env/charts/composite/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: composite
  labels:
    app.kubernetes.io/name: composite
    helm.sh/chart: composite-1.0.0
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: composite
